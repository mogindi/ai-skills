# Local bootstrap inputs for ai-skills.
# Copy to .env.local only if future repo-local scripts need it. Never commit .env.local, private keys, or tokens.

# Token used by the dispatch workflow setup when managed locally.
PORT_IO_POC_DISPATCH_TOKEN=""

# Optional read token only if this repository becomes private.
AI_SKILLS_READ_TOKEN=""
